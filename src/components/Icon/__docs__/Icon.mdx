import { Canvas, Meta } from '@storybook/blocks';
import * as Icon from './Icon.stories.tsx';

<Meta of={Icon} title="Icon" />

# Icon

Icon component

#### Example

<Canvas of={Icon} />

## Usage

```ts
CHANGE ME!!!!
import { Button } from "@femmecubator/equity-ui";

const Example = () => {
  return (
      <Button
        onClick={()=> console.log("Clicked")}
      >
      Button!
      </Button>
  );
};

export default Example;
```

#### Arguments

fill me in
